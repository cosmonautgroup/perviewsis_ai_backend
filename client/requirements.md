## Packages
(none needed)

## Notes
All necessary packages including `recharts`, `date-fns`, and `lucide-react` are already in the environment.
The application connects to the controller via `/api/connect` and manages state based on this session.
